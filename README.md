# AI-Markdown-File

## Documentation

This repository contains guidance for the team and AI assistants.

- `CLAUDE.md` — core project rules, conventions, and collaboration guidance.
- `docs/architecture.md` — architecture overview for the Hono backend, PostgreSQL on Neon, and social media integration.
- `docs/api-conventions.md` — API design patterns, request/response contracts, and error handling.

## Notes

- Use `npm run test` for backend tests.
- Use `npm run test:ui` for Vitest UI/integration tests.
- Use `npm run lint` before committing.
